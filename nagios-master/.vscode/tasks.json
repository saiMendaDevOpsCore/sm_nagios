{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "git add -A ; git commit -m Upload-COde; git push origin master",
            "type": "shell",
            "command": "git add -A ; git commit -m Upload-COde; git push origin master"
        }
    ]
}